defaults:
  - model: gat
  - experiment: gat_cora
  - _self_

seed: 42
device: auto            # auto | cpu | cuda
output_dir: outputs
hydra:
  run:
    dir: ${output_dir}/${experiment.name}/${now:%Y%m%d_%H%M%S}
  job:
    chdir: true
