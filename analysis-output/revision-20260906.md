# Revision Progress Note (2026-09-06)

State of the IJPR-AI revision work after Colab batches B1/B2/B3 (2026-09-05)
and the S4 local stage (merge + dedup + final_stats, 2026-09-06).

## What was done

1. **Merge (S4a)**: `temp/merge_colab_20260905.py` merged 450 new-protocol rows
   into `outputs/tables/results_clean.csv` (now 726 rows: 276 old + 450 new;
   backup `results_clean_premerge_20260820.csv`). 40 old cora pruning rows
   dropped as semantic duplicates of the new B3 `best`-source rows.
2. **Dedup check (S4b)**: `late_min_div` == `late_last` in 119/120 (dataset,
   seed, criterion) combinations — similarity decreases monotonically under
   the controller. The one exception (citeseer seed 4, magnitude) is real
   (min-div at epoch 98 vs final 100, 0.3 pp apart).
3. **final_stats (S4c)**: `run/pipeline/final_stats.py` extended with protocol
   guards (old sections select `lambda_schedule=='' and checkpoint_source==''`)
   and new sections R1 (adaptive baselines) / R2 (revival test).
   `analysis-output/stats-appendix.md` regenerated.
4. **Paper**: new tables `paper/tables/tab4_adaptive_baselines.tex` and
   `tab5_revival.tex` (generated by `paper/make_tables_rev.py`), new figure
   `paper/figures/fig4_adaptive_baselines.pdf` (added to
   `paper/make_figures_pub.py`), and `paper/main.tex` updated:
   - H1: adaptive-baseline comparison (controller ties linear/Kendall, beats
     GradNorm +1.75 pp cora p=0.010 / +1.37 pp pubmed p=0.005; GradNorm reaches
     the budget already at the best checkpoint).
   - H2: revival test — all 9 late-source contrasts null (one-sided p>=0.078),
     best checkpoint never worse than late (citeseer: best +1.90 pp, p=0.001).
   - Limitations: revival wording updated; provenance note covers B1-B3.
   - Abstract trimmed to <=200 words (199 by local counter).

## Key results (R1/R2)

- R1 controller arm = submitted dense adaptive block (distill=False). NOTE:
  rows with `distill=True` and `prune_criterion==''` are B5 pure-KD students
  (test_acc = student), not teachers — never mix them into controller stats.
- R1: controller vs linear/kendall: all ns (|Δ|<=0.48 pp); vs gradnorm:
  +1.75 cora (p=0.010), +1.37 pubmed (p=0.005), +1.10 citeseer (p=0.053).
  gradnorm λ_final≈1.95 everywhere (renormalization bound), d_best=0.69 on
  cora/citeseer (enforces early), 1.00 on pubmed (saturates, no effect).
- R2: 3 checkpoint sources × 4 criteria × 10 seeds × 3 datasets at ratio 0.75.
  best-source d_src 0.93-0.99, late-source 0.71-0.99. Revival FAILS: no
  contrast significant; nominally largest effect favors random (-0.87 pubmed).
  Pubmed is a weak probe (late d_src=0.99; controller ceiling-bound).

## Validation

- `python paper/check_main_tex.py` PASSES (env balance, cite keys, labels,
  paths). No TeX distribution on this machine — not compiled.

## Second round (2026-09-06, afternoon)

5. **M4 cut**: Multi30k subsection, its non-claims sentence, the setup
   "5-6 auxiliary seeds" clause, and limitations (2)/(6) removed; limitations
   now note GAT-only evidence and the pending code URL (M6 placeholder).
6. **H2 data switch**: the adaptive-teacher ratio-0.75 contrasts now use the
   B3 `best`-source rows (+0.05 / -0.13 / +0.01 pp; one-sided p=0.28/0.63/0.54)
   because the pre-merge rows were dropped in the merge. tab2 regenerated from
   the CSV (paper/make_tables_rev.py tab2()).
7. **M3 wall time**: B5 vs B8 wall times essentially equal (7.9 vs 8.8 s at
   ratio 0.5; 8.4 vs 8.3 s at 0.75; both include teacher training). The
   "cheaper re-training" angle is dropped; the pruning path's value is
   structural (reuses trained dense weights).
8. **M5 tie statistics**: B8-vs-B5 now reported two-sided with 95% CIs on the
   difference (-0.48 pp [-1.12, +0.16] p=0.19 at 0.5; -0.84 pp [-1.79, +0.11]
   p=0.13 at 0.75; t-based CIs, both include zero); wording upgraded from
   "tied" to "no significant difference detectable, not equivalence" everywhere
   (abstract, contributions, non-claims, H3, fig3 caption, conclusion,
   limitations).
9. **Setup/Method/Appendix**: setup now states the two-sided convention for tie
   claims and the B1-B3 notebook provenance; Method documents the checkpoint
   sources; Appendix A gains tab6_revision_cis.tex (R1/R2 95% CIs).
10. Abstract re-trimmed to 198 words (local counter). All checks pass.

## Compile (2026-09-06, evening)

- TeX Live 2026 scheme-basic installed locally at `C:\Users\hukej\texlive\2026`
  (TUNA mirror); extra packages: xcolor, wrapfig, cite (+ overcite.sty fetched
  from CTAN into texmf-local, not in TeX Live). `\etal` defined in main.tex
  (was undefined).
- Build command (from paper/): pdflatex main → bibtex main → pdflatex ×2.
  Result: 0 errors, 0 overfull boxes, no undefined refs/citations, 15 pages.
- Table numbering (auto): Table 1 fixed-λ; Table 2 adaptive baselines;
  Table 3 criteria matrix; Table 4 revival test; Table 5 KD; Table 6 main CIs;
  Table 7 revision CIs. Figures: Fig. 1 H1 bars, Fig. 2 baselines, Fig. 3
  traces, Fig. 4 KD.
- Minor abstract/λ_max polish also landed (abstract 198 words, "on the
  datasets we test" qualifier, λ_max stability sentence).

## Next steps

- [ ] M6: fill the code URL (placeholder in contribution 4 / limitation 6).
- [ ] Refresh the submission zip (ijprai-submission-*.zip) with the revised
      main.tex + new tables/figures when ready to resubmit.

## Third round (2026-09-07)

11. **Sensitivity merge + tab7**: the 12 `outputs_rev2_*` CSVs (179 rows:
    8 controller sensitivity cells on Cora, 1 Pubmed eta=0.2 cell, the
    fixed-λ re-run sweep, and the Citeseer/Pubmed λ=5 runs) merged into
    `results_clean.csv` (726->905) by `temp/merge_outputs_rev2_20260907.py`.
    tab7 filled with real numbers: the 9th cell (eta=0.05, d*=0.7) is the
    submitted main controller arm (80.79 ± 0.92); the other 8 cells are the
    outputs_rev2 runs (10 seeds each).
12. **B4 + H3-extension batch (Colab)**: B4 from-scratch small models
    (4/2 heads, no KD, 100 epochs; 3 datasets x 2 ratios x 10 seeds), plus
    Citeseer/Pubmed B5 (pure KD) / B6@0.5 (prune+FT) / B8 (prune+KD) arms —
    162 new rows merged (905->1067) by
    `temp/merge_h3ext_colab_20260907.py`. B6@0.75 for Citeseer/Pubmed reuses
    the B3 `best` rows. Code: `train.py` now tolerates a ratio-only prune cfg
    (B4 head-budget labeling); `distill.py` writes the new-protocol fields
    (lambda_schedule/checkpoint_source/criterion_div).
13. **H3 paragraph + tab8 filled**: Cora B4 = 80.73 ± 1.36 (4/8 heads) /
    80.60 ± 1.05 (2/8 heads) — prune+FT (B6) sits 0.8–0.9 pp BELOW the
    from-scratch small model, so the H3 narrative changed: the fine-tuning
    gap below B8 is not just the small architecture; pruning+plain-FT is a
    small net loss vs. matched from-scratch training, and KD recovers it.
    tab8_h3ext now reports all four arms for Citeseer/Pubmed (KD-recovery
    pattern holds: B8 > B6 everywhere, B8 ≈ B5).
14. **Compile + zip**: 0 errors, 0 overfull, 18 pages; provenance sentence
    updated (H3 extension ran on Colab, not locally).
    `ijprai-submission-20260907.zip` (repo root, 16 files) built by
    `temp/make_submission_zip.py`; SUBMISSION-README.md file table updated.
